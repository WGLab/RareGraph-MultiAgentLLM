# =============================================================================
# RareDisGraph-MultiAgentLLM — Python dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# GPU / CUDA note:
#   vLLM requires CUDA 11.8+. If the version below conflicts with your CUDA,
#   follow https://docs.vllm.ai/en/latest/getting_started/installation.html
#   and install vLLM separately before running pip install -r requirements.txt.
# =============================================================================

# ----- LLM inference ---------------------------------------------------------
vllm>=0.4.0                     # local LLM serving with PagedAttention
transformers>=4.40.0            # model loading / tokenization
accelerate>=0.29.0              # multi-GPU / device placement helpers
torch>=2.1.0                    # PyTorch (CUDA build installed separately)

# ----- Vision pathway --------------------------------------------------------
Pillow>=10.0.0                  # image loading and preprocessing
torchvision>=0.16.0             # vision utilities (used by VL model)

# ----- Ontology & knowledge graph --------------------------------------------
pronto>=2.5.0                   # hp.obo / mondo.obo parsing
networkx>=3.2.0                 # graph operations (PageRank, traversal)
obonet>=1.0.0                   # alternative OBO file reader

# ----- Biomedical ID mapping / phenotype processing --------------------------
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0                   # statistical utilities

# ----- VCF handling ----------------------------------------------------------
cyvcf2>=0.30.0                  # fast VCF parsing

# ----- Fuzzy string matching (MONDO / disease-name normalization) ------------
rapidfuzz>=3.6.0                # used for PhenoBrain / DeepRare MONDO normalization

# ----- Configuration & CLI ---------------------------------------------------
pyyaml>=6.0.1                   # YAML config loading
omegaconf>=2.3.0                # config merging / overrides
click>=8.1.0                    # CLI argument parsing
tqdm>=4.66.0                    # progress bars

# ----- Output & evaluation ---------------------------------------------------
jsonlines>=4.0.0                # streaming JSON output
tabulate>=0.9.0                 # pretty-print evaluation tables

# ----- Utilities -------------------------------------------------------------
requests>=2.31.0                # optional: PubCaseFinder API calls
python-dotenv>=1.0.0            # environment variable loading
loguru>=0.7.0                   # structured logging
